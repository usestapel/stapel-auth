name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install stapel-core
        run: pip install git+https://github.com/usestapel/stapel-core.git

      - name: Install stapel-gdpr
        run: pip install git+https://github.com/usestapel/stapel-gdpr.git

      - name: Install package with all extras
        run: pip install ".[all]"

      - name: Install test dependencies
        run: pip install pytest pytest-django pytest-cov python3-openid

      - name: Hide local sub-packages that shadow installed packages
        # The flat layout places openid/ at the repo root. If the repo root ends up
        # in sys.path (via pytest's conftest traversal through tests/__init__.py),
        # `import openid` finds this local sub-package instead of python3-openid.
        # Renaming it after pip install ensures only the installed package is found.
        run: mv openid _openid_pkg_src

      - name: Lint
        run: |
          pip install ruff --quiet
          ruff check . --select E,F,W --ignore E501

      - name: emit-check (outbox atomicity gate)
        run: python -m stapel_core.lint.emit_check .

      - name: Import check
        run: python -c "import stapel_auth; print('import OK')"

      - name: Tests with coverage
        run: |
          python -m pytest --pyargs stapel_auth.tests \
            --cov=stapel_auth \
            --cov-report=term-missing \
            --cov-report=xml \
            --junitxml=junit.xml \
            -o junit_family=legacy \
            -v

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        if: ${{ !cancelled() }}
        continue-on-error: true
        with:
          files: coverage.xml
          flags: stapel-auth
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload test results to Codecov
        uses: codecov/test-results-action@v1
        if: ${{ !cancelled() }}
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  release-track:
    # Release gate — library-standard §7.4. Installs the package the way an end
    # user does: dependencies (incl. stapel-core) resolved from PyPI strictly by
    # the declared pins — no git-main core, no editable siblings. Catches "pin
    # into the void" and drift between the core branch and the published wheel.
    # Advisory (continue-on-error) until the whole stapel dependency graph is
    # published on PyPI at compatible versions; drop continue-on-error to make a
    # green run the blocking precondition for a vX.Y.Z tag.
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install as an end user (deps resolved from PyPI by pin)
        run: pip install .
      - name: Verify stapel-core resolved from PyPI inside the 0.8 window
        run: cd "$RUNNER_TEMP" && python -c "import importlib.metadata as m; v=m.version('stapel-core'); print('stapel-core', v); assert tuple(map(int, v.split('.')[:2]))==(0,8), v"
      - name: Import smoke (installed package, from a clean dir)
        run: cd "$RUNNER_TEMP" && python -c "import stapel_auth; print('release-track import OK')"
